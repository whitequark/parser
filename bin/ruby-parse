#!/usr/bin/env ruby

abort("Parser's CLI requires at least Ruby 2.0") if RUBY_VERSION < '2.0.0'

$LOAD_PATH.unshift(File.expand_path('../../lib', __FILE__))
require 'parser/runner/ruby_parse'

Parser::Runner::RubyParse.go(ARGV)
